// подтягиваем gtest/gtest.h чтобы здесь были нужные типы и функции
#include <gtest/gtest.h>

// подтягиваем optional чтобы здесь были нужные типы и функции
#include <optional>
// подтягиваем string чтобы здесь были нужные типы и функции
#include <string>
// подтягиваем unordered_map чтобы здесь были нужные типы и функции
#include <unordered_map>
// подтягиваем unordered_set чтобы здесь были нужные типы и функции
#include <unordered_set>
// подтягиваем vector чтобы здесь были нужные типы и функции
#include <vector>

// подтягиваем online/MarketDataConnectors.h чтобы здесь были нужные типы и функции
#include "online/MarketDataConnectors.h"

// эта функция test_case решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// этим тестом проверяем отдельный рабочий сценарий
TEST(TradingViewQuotes, PrefersRowsWithBidAskAndAppliesFeeMap) {
    // тут коротко поясняем зачем нужен этот кусок
// сохраняем значение в переменную чтобы использовать дальше
    std::vector<am::TradingViewRowCandidate> rows = {
// здесь проводим маленький но важный технический шаг
        {"BINANCE", "XTZUSD", std::nullopt, std::nullopt, 5.0},
// этот кусок нужен чтобы код шел по понятному сценарию
        {"BINANCE", "XTZUSD", 4.90, 5.10, 5.0},
// этот кусок нужен чтобы код шел по понятному сценарию
        {"KRAKEN", "XTZUSD", 5.00, 5.20, 5.1},
// закрываем объявление типа и заканчиваем этот блок
    };

// фиксируем значение чтобы дальше случайно его не менять
    const std::unordered_map<std::string, double> fees = {
// здесь проводим маленький но важный технический шаг
        {"BINANCE", 7.5},
// закрываем объявление типа и заканчиваем этот блок
    };

// эта функция build_tradingview_quotes решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// фиксируем значение чтобы дальше случайно его не менять
    const auto quotes = am::MarketDataConnectors::build_tradingview_quotes(
// делаем небольшой шаг и готовим почву для следующих строк
        rows, fees, 10.0, {"XTZUSD"}, {"BINANCE"});

// эта функция ASSERT_EQ решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    ASSERT_EQ(quotes.size(), 1u);
// эта функция EXPECT_EQ решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_EQ(quotes[0].exchange, "BINANCE");
// эта функция EXPECT_EQ решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_EQ(quotes[0].symbol, "XTZUSD");
// эта функция EXPECT_NEAR решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_NEAR(quotes[0].bid, 4.90, 1e-12);
// эта функция EXPECT_NEAR решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_NEAR(quotes[0].ask, 5.10, 1e-12);
// эта функция EXPECT_NEAR решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_NEAR(quotes[0].fee_bps, 7.5, 1e-12);
// закрываем текущий блок и поднимаемся уровнем выше
}

// эта функция test_case решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// этим тестом проверяем отдельный рабочий сценарий
TEST(TradingViewQuotes, FallsBackToCloseWhenBidAskMissing) {
    // тут по делу отмечаем зачем написана следующая часть
// сохраняем значение в переменную чтобы использовать дальше
    std::vector<am::TradingViewRowCandidate> rows = {
// здесь проводим маленький но важный технический шаг
        {"KRAKEN", "APTUSD", std::nullopt, std::nullopt, 12.34},
// закрываем объявление типа и заканчиваем этот блок
    };

// эта функция build_tradingview_quotes решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// фиксируем значение чтобы дальше случайно его не менять
    const auto quotes = am::MarketDataConnectors::build_tradingview_quotes(
// делаем небольшой шаг и готовим почву для следующих строк
        rows, {}, 10.0, {"APTUSD"}, {});

// эта функция ASSERT_EQ решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    ASSERT_EQ(quotes.size(), 1u);
// эта функция EXPECT_EQ решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_EQ(quotes[0].exchange, "KRAKEN");
// эта функция EXPECT_EQ решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_EQ(quotes[0].symbol, "APTUSD");
// эта функция EXPECT_NEAR решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_NEAR(quotes[0].bid, 12.34, 1e-12);
// эта функция EXPECT_NEAR решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_NEAR(quotes[0].ask, 12.34, 1e-12);
// эта функция EXPECT_NEAR решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_NEAR(quotes[0].fee_bps, 10.0, 1e-12);
// закрываем текущий блок и поднимаемся уровнем выше
}

// эта функция test_case решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// этим тестом проверяем отдельный рабочий сценарий
TEST(TradingViewQuotes, FiltersOutRowsOutsideRequestedSymbols) {
    // здесь оставляем понятную подсказку по текущему шагу
// сохраняем значение в переменную чтобы использовать дальше
    std::vector<am::TradingViewRowCandidate> rows = {
// здесь проводим маленький но важный технический шаг
        {"BINANCE", "XTZUSD", 4.90, 5.10, 5.0},
// тут просто продолжаем собирать итоговый результат
        {"BINANCE", "ARBUSD", 1.00, 1.05, 1.02},
// закрываем объявление типа и заканчиваем этот блок
    };

// эта функция build_tradingview_quotes решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// фиксируем значение чтобы дальше случайно его не менять
    const auto quotes = am::MarketDataConnectors::build_tradingview_quotes(
// делаем небольшой шаг и готовим почву для следующих строк
        rows, {}, 10.0, {"ARBUSD"}, {});

// эта функция ASSERT_EQ решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    ASSERT_EQ(quotes.size(), 1u);
// эта функция EXPECT_EQ решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    EXPECT_EQ(quotes[0].symbol, "ARBUSD");
// закрываем текущий блок и поднимаемся уровнем выше
}


