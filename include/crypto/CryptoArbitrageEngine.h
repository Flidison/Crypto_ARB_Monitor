// ставим защиту от повторного подключения заголовка
#pragma once

// подтягиваем optional чтобы здесь были нужные типы и функции
#include <optional>
// подтягиваем string чтобы здесь были нужные типы и функции
#include <string>
// подтягиваем unordered_map чтобы здесь были нужные типы и функции
#include <unordered_map>
// подтягиваем unordered_set чтобы здесь были нужные типы и функции
#include <unordered_set>
// подтягиваем vector чтобы здесь были нужные типы и функции
#include <vector>

// заходим в пространство имен чтобы не засорять глобальную область
namespace am {

// здесь оставляем понятную подсказку по текущему шагу
// этот struct CryptoQuote нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// тут делаем очередной рабочий шаг и двигаемся дальше
struct CryptoQuote {
// делаем небольшой шаг и готовим почву для следующих строк
    std::string exchange;
// делаем небольшой шаг и готовим почву для следующих строк
    std::string symbol;
// сохраняем значение в переменную чтобы использовать дальше
    double bid = 0.0;
// сохраняем значение в переменную чтобы использовать дальше
    double ask = 0.0;
// сохраняем значение в переменную чтобы использовать дальше
    double fee_bps = 0.0;
// закрываем объявление типа и заканчиваем этот блок
};

// этот struct CryptoOpportunity нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// этот кусок нужен чтобы код шел по понятному сценарию
struct CryptoOpportunity {
// делаем небольшой шаг и готовим почву для следующих строк
    std::string symbol;
// делаем небольшой шаг и готовим почву для следующих строк
    std::string buy_exchange;
// делаем небольшой шаг и готовим почву для следующих строк
    std::string sell_exchange;
// сохраняем значение в переменную чтобы использовать дальше
    double buy_ask = 0.0;
// сохраняем значение в переменную чтобы использовать дальше
    double sell_bid = 0.0;
// сохраняем значение в переменную чтобы использовать дальше
    double gross_spread = 0.0;
// сохраняем значение в переменную чтобы использовать дальше
    double net_spread = 0.0;
// сохраняем значение в переменную чтобы использовать дальше
    double net_pct = 0.0;
// закрываем объявление типа и заканчиваем этот блок
};

// этот struct CryptoMonitorConfig нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// этой строкой фиксируем промежуточный результат
struct CryptoMonitorConfig {
// сохраняем значение в переменную чтобы использовать дальше
    std::string symbol_filter = "BTCUSD";
// делаем небольшой шаг и готовим почву для следующих строк
    std::unordered_set<std::string> allowed_exchanges;
// сохраняем значение в переменную чтобы использовать дальше
    double default_fee_bps = 10.0;
// сохраняем значение в переменную чтобы использовать дальше
    double min_net_spread = 0.0;
// сохраняем значение в переменную чтобы использовать дальше
    double min_net_pct = 0.0;
// закрываем объявление типа и заканчиваем этот блок
};

// этот class IArbitrageEngine нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// тут просто продолжаем собирать итоговый результат
class IArbitrageEngine {
// тут лежит публичный интерфейс который видят снаружи
public:
// эта функция ~IArbitrageEngine решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// сохраняем значение в переменную чтобы использовать дальше
    virtual ~IArbitrageEngine() = default;

// эта функция load_quotes_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// тут закрываем небольшой кусок общей задачи
    virtual std::vector<CryptoQuote> load_quotes_csv(
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& csv_path,
// фиксируем значение чтобы дальше случайно его не менять
        const std::unordered_map<std::string, double>& per_exchange_fee_bps,
// сохраняем значение в переменную чтобы использовать дальше
        double default_fee_bps) const = 0;

// эта функция load_fees_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// сохраняем значение в переменную чтобы использовать дальше
    virtual std::unordered_map<std::string, double> load_fees_csv(const std::string& csv_path) const = 0;

// эта функция find_opportunities решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// тут закрываем небольшой кусок общей задачи
    virtual std::vector<CryptoOpportunity> find_opportunities(
// фиксируем значение чтобы дальше случайно его не менять
        const std::vector<CryptoQuote>& quotes,
// фиксируем значение чтобы дальше случайно его не менять
        const CryptoMonitorConfig& cfg) const = 0;

    // этим комментом помогаем быстрее считать логику глазами
// эта функция reset_opportunities_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// сохраняем значение в переменную чтобы использовать дальше
    virtual void reset_opportunities_csv(const std::string& path) const = 0;
// эта функция append_opportunities_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// этот кусок нужен чтобы код шел по понятному сценарию
    virtual void append_opportunities_csv(
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& path,
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& observed_at,
// фиксируем значение чтобы дальше случайно его не менять
        const std::vector<CryptoOpportunity>& opps) const = 0;
// эта функция reset_profit_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// сохраняем значение в переменную чтобы использовать дальше
    virtual void reset_profit_csv(const std::string& path) const = 0;
// эта функция append_profit_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// этот кусок нужен чтобы код шел по понятному сценарию
    virtual double append_profit_csv(
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& path,
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& observed_at,
// этот кусок нужен чтобы код шел по понятному сценарию
        double capital_before,
// фиксируем значение чтобы дальше случайно его не менять
        const std::vector<CryptoOpportunity>& opps) const = 0;

// эта функция write_opportunities_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// тут делаем очередной рабочий шаг и двигаемся дальше
    virtual void write_opportunities_csv(
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& path,
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& observed_at,
// фиксируем значение чтобы дальше случайно его не менять
        const std::vector<CryptoOpportunity>& opps) const = 0;
// закрываем объявление типа и заканчиваем этот блок
};

// этот class CryptoArbitrageEngine нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// этот кусок нужен чтобы код шел по понятному сценарию
class CryptoArbitrageEngine final : public IArbitrageEngine {
// тут лежит публичный интерфейс который видят снаружи
public:
// эта функция load_quotes_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// тут закрываем небольшой кусок общей задачи
    std::vector<CryptoQuote> load_quotes_csv(
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& csv_path,
// фиксируем значение чтобы дальше случайно его не менять
        const std::unordered_map<std::string, double>& per_exchange_fee_bps,
// делаем небольшой шаг и готовим почву для следующих строк
        double default_fee_bps) const override;

// эта функция load_fees_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    std::unordered_map<std::string, double> load_fees_csv(const std::string& csv_path) const override;

// эта функция find_opportunities решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// тут закрываем небольшой кусок общей задачи
    std::vector<CryptoOpportunity> find_opportunities(
// фиксируем значение чтобы дальше случайно его не менять
        const std::vector<CryptoQuote>& quotes,
// фиксируем значение чтобы дальше случайно его не менять
        const CryptoMonitorConfig& cfg) const override;

// эта функция reset_opportunities_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    void reset_opportunities_csv(const std::string& path) const override;
// эта функция append_opportunities_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// здесь проводим маленький но важный технический шаг
    void append_opportunities_csv(
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& path,
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& observed_at,
// фиксируем значение чтобы дальше случайно его не менять
        const std::vector<CryptoOpportunity>& opps) const override;
// эта функция reset_profit_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    void reset_profit_csv(const std::string& path) const override;
// эта функция append_profit_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// здесь проводим маленький но важный технический шаг
    double append_profit_csv(
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& path,
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& observed_at,
// здесь проводим маленький но важный технический шаг
        double capital_before,
// фиксируем значение чтобы дальше случайно его не менять
        const std::vector<CryptoOpportunity>& opps) const override;

// эта функция write_opportunities_csv решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// тут просто продолжаем собирать итоговый результат
    void write_opportunities_csv(
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& path,
// фиксируем значение чтобы дальше случайно его не менять
        const std::string& observed_at,
// фиксируем значение чтобы дальше случайно его не менять
        const std::vector<CryptoOpportunity>& opps) const override;
// закрываем объявление типа и заканчиваем этот блок
};

// здесь аккуратно готовим данные для следующего шага
} // закрываем пространство имен am


