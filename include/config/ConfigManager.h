// ставим защиту от повторного подключения заголовка
#pragma once
// подтягиваем optional чтобы здесь были нужные типы и функции
#include <optional>
// подтягиваем string чтобы здесь были нужные типы и функции
#include <string>
// подтягиваем unordered_map чтобы здесь были нужные типы и функции
#include <unordered_map>

// заходим в пространство имен чтобы не засорять глобальную область
namespace am {

// тут коротко поясняем зачем нужен этот кусок
// этот class IConfigManager нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// тут просто продолжаем собирать итоговый результат
class IConfigManager {
// тут лежит публичный интерфейс который видят снаружи
public:
// эта функция ~IConfigManager решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// сохраняем значение в переменную чтобы использовать дальше
    virtual ~IConfigManager() = default;

// эта функция load решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// сохраняем значение в переменную чтобы использовать дальше
    virtual void load() = 0;

// эта функция get_string решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// сохраняем значение в переменную чтобы использовать дальше
    virtual std::optional<std::string> get_string(const std::string& key) const = 0;
// эта функция get_double решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// сохраняем значение в переменную чтобы использовать дальше
    virtual std::optional<double> get_double(const std::string& key) const = 0;
// эта функция get_bool решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// сохраняем значение в переменную чтобы использовать дальше
    virtual std::optional<bool> get_bool(const std::string& key) const = 0;
// закрываем объявление типа и заканчиваем этот блок
};

// этот class ConfigManager нужен чтобы держать рядом данные и связанную логику
// внутри мы собираем единый контракт чтобы модуль было проще читать и расширять
// тут просто продолжаем собирать итоговый результат
class ConfigManager final : public IConfigManager {
// тут лежит публичный интерфейс который видят снаружи
public:
    // этой строкой подсказываем смысл соседнего блока
// эта функция ConfigManager решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    explicit ConfigManager(std::string path);

// эта функция load решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    void load() override;

// эта функция get_string решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    std::optional<std::string> get_string(const std::string& key) const override;
// эта функция get_double решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    std::optional<double> get_double(const std::string& key) const override;
// эта функция get_bool решает отдельную задачу внутри общего потока работы
// по шагам берем вход, делаем проверки и отдаем ожидаемый результат наружу
// делаем небольшой шаг и готовим почву для следующих строк
    std::optional<bool> get_bool(const std::string& key) const override;

// здесь прячем внутренние детали чтобы не трогать их снаружи
private:
// делаем небольшой шаг и готовим почву для следующих строк
    std::string path_;
// делаем небольшой шаг и готовим почву для следующих строк
    std::unordered_map<std::string, std::string> kv_;
// закрываем объявление типа и заканчиваем этот блок
};

// здесь проводим маленький но важный технический шаг
} // закрываем пространство имен am


